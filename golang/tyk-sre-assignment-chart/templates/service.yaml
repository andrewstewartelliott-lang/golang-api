apiVersion: v1
kind: Service
metadata:
  name: {{ include "tyk-sre-assignment-chart.fullname" . }}
  labels:
    {{- include "tyk-sre-assignment-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tyk-sre-assignment-chart.selectorLabels" . | nindent 4 }}
